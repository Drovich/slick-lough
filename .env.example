# Supabase — récupère ces valeurs dans ton projet Supabase (Settings > API)
PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Hash SHA-256 des mots de passe — à générer avec :
#   node -e "crypto.subtle.digest('SHA-256', new TextEncoder().encode('MOT_DE_PASSE')).then(b => console.log([...new Uint8Array(b)].map(x => x.toString(16).padStart(2,'0')).join('')))"
PUBLIC_PARTICIPANT_PASSWORD_HASH=...
PUBLIC_ARBITRE_PASSWORD_HASH=...
PUBLIC_ADMIN_PASSWORD_HASH=...
